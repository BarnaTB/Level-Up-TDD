language: python
python:
  - "3.5.2"
install:
  - pip install -r requirements.txt
  - pip install python-coveralls
  - pip install nose

script:
  - nosetests --with-coverage
  - coverage report

after_success: 
  - coveralls